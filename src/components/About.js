import React from "react";

const About = () => {
  return (
    <div className="page">
      <h1>About Page (Coming Soon)</h1>
    </div>
  );
};

export default About;
// The About component is a functional component that displays a placeholder message indicating that the about page is coming soon. The component is imported and used in the App component to display the about page when the corresponding route is matched. The styles.css file is also imported to apply styling to the about page.